@model IEnumerable<RLIM.UI.Models.QualityModel>

@{
    ViewData["Title"] = "Qualities";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Name)</th>
            <th>@Html.DisplayNameFor(model => model.Rank)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td></td>
            <td></td>
            <td><a asp-controller="Quality" asp-action="Create" class="btn btn-primary">New Quality</a></td>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Rank)</td>
                <td>
                    <a asp-controller="Quality" asp-action="Update" asp-route-id="@item.ID" class="btn btn-warning">Update</a>
                    <a asp-controller="Quality" asp-action="Delete" asp-route-id="@item.ID" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
