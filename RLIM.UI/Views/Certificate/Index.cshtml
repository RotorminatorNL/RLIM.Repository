@model IEnumerable<RLIM.UI.Models.CertificateModel>

@{
    ViewData["Title"] = "Certificates";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Name)</th>
            <th>@Html.DisplayNameFor(model => model.Tier)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td></td>
            <td></td>
            <td><a asp-controller="Certificate" asp-action="Create" class="btn btn-primary">New Certificate</a></td>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Tier)</td>
                <td>
                    <a asp-controller="Certificate" asp-action="Update" asp-route-id="@item.ID" class="btn btn-warning">Update</a>
                    &nbsp;
                    <a asp-controller="Certificate" asp-action="Delete" asp-route-id="@item.ID" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
